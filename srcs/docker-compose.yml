version: "3"


services:
  mariadb:
    build: mariadb/
    volumes:
      - db:/var/lib/mysql
    networks:
      App:
        ipv4_address: 10.10.10.2
    env_file:
      - .env



volumes:
  www:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/dbdata/www'
  db:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/dbdata/db'


networks:
  App:
    ipam:
      driver: default
      config:
        - subnet: "10.10.10.0/24"
